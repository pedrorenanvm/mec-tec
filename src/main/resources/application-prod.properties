spring.application.name=mec-tec

# Constr�i a URL do banco de dados usando as vari�veis de ambiente do Koyeb.
# A porta padr�o do PostgreSQL (5432) � adicionada aqui.
spring.datasource.url=jdbc:postgresql://${DATABASE_HOST}:5432/${DATABASE_NAME}

# L� o usu�rio e a senha diretamente das vari�veis de ambiente do Koyeb.
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}

# Configura��es recomendadas para produ��o.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false